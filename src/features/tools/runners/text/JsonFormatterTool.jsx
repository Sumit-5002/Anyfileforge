import React from 'react';
import TextToolRunner from '../../../../components/tools/TextToolRunner';

function JsonFormatterTool({ tool }) {
    return <TextToolRunner tool={tool} />;
}

export default JsonFormatterTool;
